import { NgxOnHoverDirective } from './ngx-on-hover.directive';

describe('NgxOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
